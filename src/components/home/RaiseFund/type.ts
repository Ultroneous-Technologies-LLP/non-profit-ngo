import { Image, ViewMoreButton } from "@/types";

export interface RaiseFundProps {
  background: Image;
  description: string;
  enrollButton: ViewMoreButton;
  fundForNGOButton: ViewMoreButton;
  image: Image;
  title: string;
}
